version: '3'
services:
    front-end:
        build: .
        image: moviesurfer:latest
        command: npm run dev
        ports:
          - '3000:3000'
        volumes:
          - '.:/usr/src/app'
          - /usr/src/app/node_modules
