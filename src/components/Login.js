import React, { Component } from 'react';

class Login extends Component {
    render() {
        return (
            <form>
                <fieldset>
                    <legend>Login</legend>
                </fieldset>
            </form>
        );
    }
}

export default Login;
